# Visuels

Déposer ici les fichiers fournis :

- `johane-achouri.jpg` — photo du fondateur (carré recommandé, ≥ 600×600).
  Une fois ajoutée, remplacer le placeholder à initiales par un `<Image>`
  dans `components/home/Fondateur.tsx` et `app/a-propos/page.tsx`.
- `og.jpg` — image Open Graph pour les partages (1200×630), optionnel.
- Logo / favicon, optionnel.
